spring:
  application:
    name: weather-app
server:
  port: 8080
# set this to your actual key or set an environment variable
openweather:
  api:
  key: ${OPENWEATHER_API_KEY:}
  base-url: https://api.openweathermap.org


# Optional: logging
logging:
  level:
    root: INFO